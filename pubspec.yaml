name: his_medica_itsur_app
description: Historia Clínica ITSUR - NOM-004

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  provider: ^6.1.1
  intl: ^0.19.0
  uuid: ^4.5.0
  shared_preferences: ^2.2.2  # ← NUEVA

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/composition_template.json